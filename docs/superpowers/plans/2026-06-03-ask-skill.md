# Skill `ask` (AMA Produk Read-Only) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tambah satu skill baru `ask` ke plugin context-vault — pintu tanya-jawab read-only tentang produk yang sedang dikerjakan (knowledge-first, code-fallback, flag-drift→route, tanpa nulis).

**Architecture:** `ask` adalah skill markdown (SKILL.md) seperti skill lain di `plugin/skills/`. Implementasi = (1) tulis `SKILL.md` baru, (2) daftarkan di dua manifest plugin, (3) update README + spec induk. Tidak ada kode runtime, tidak ada perubahan ke skill lain — `ask` murni baca. "Test" di sini = verifikasi struktural (frontmatter valid, name==dir, JSON manifest valid, hitungan skill konsisten) karena artifact-nya dokumen, bukan kode eksekusi.

**Tech Stack:** Markdown (SKILL.md, frontmatter YAML), JSON (plugin.json, marketplace.json), `python3` (validator JSON/YAML, sudah ada di macOS), `git`.

**Referensi spec:** `docs/superpowers/specs/2026-06-03-ask-skill-design.md` (§4 alur, §5 grounding/drift, §6 guardrails, §7 trigger, §8 dampak komponen).

---

### Task 1: Tulis `plugin/skills/ask/SKILL.md`

**Files:**
- Create: `plugin/skills/ask/SKILL.md`

Skill ini meniru struktur `plugin/skills/render-docs/SKILL.md` (frontmatter `name`/`description` → `# Judul` → `Tujuan` → `## section` ber-`###` langkah → `## Catatan`). Description mengikuti pola rumah: `Use untuk … — … Trigger — "…". Jalankan dari root produk yang punya control/.`

- [ ] **Step 1: Buat folder + tulis file lengkap**

Buat `plugin/skills/ask/SKILL.md` dengan isi PERSIS berikut:

````markdown
---
name: ask
description: Use untuk nanya APA PUN tentang produk yang lagi dikerjain (greenfield/brownfield) — AMA read-only. Knowledge-first, code-fallback — baca control/ dulu, turun ke kode cuma saat level-implementasi atau knowledge tipis. Tiap jawaban sebut sumber; flag drift (catatan vs kode) lalu route ke skill pemilik. TIDAK pernah nulis. Trigger — "ask <pertanyaan>", "tanya <x>", "jelasin <x>", "status produk", "fitur apa aja yang udah jalan". Jalankan dari root produk yang punya control/.
---

# ask — Tanya apa pun tentang produk (read-only)

Tujuan: jawab pertanyaan apa pun tentang PRODUK yang sedang dikerjakan, dari knowledge `control/` (sumber kebenaran) dengan fallback ke kode saat perlu. **100% read-only** — `ask` tidak pernah menyentuh file; satu-satunya "aksi" = menyarankan/route ke skill lain.

## Prasyarat
- Jalankan dari root produk yang punya `control/`.
- **Tanpa `control/`** (folder mentah / belum init) → jangan jawab sebagai-produk: arahkan `/init` (atau `/discovery` kalau ide masih mentah), lalu STOP.

## Alur (tiap pertanyaan)

### 1. Klasifikasi → file relevan
Tentukan domain pertanyaan, buka **hanya** file yang relevan (bukan semua `control/`):

| Domain pertanyaan | Sumber `control/` |
|---|---|
| Bisnis: produk apa, pengguna, nilai, istilah, langkah flow | `business/domain.md` · `flows.md` · `glossary.md` |
| Arsitektur: app/package, tanggung jawab, stack, capability, topology | `workspace.yaml` |
| Aturan platform (tenancy/money/idempotency/authz/PII-PCI) | `invariants.md` |
| Konvensi lintas-app | `conventions.md` |
| Vendor eksternal (arah/mode/dipakai-di) | `integrations.md` |
| Status: fitur apa saja, draft/active/shipped, perilaku 1 fitur | `features/*/feature.yaml` (+ `business.md`) |
| Bug / known-issues / riwayat fix | `fixes/*/fix.yaml` (+ `notes.md`) |
| Implementasi: "fungsi X jalan gimana", "endpoint Y di mana" | → code-fallback (langkah 3) |

Pertanyaan lintas-domain → buka >1 sumber.

### 2. Baca knowledge
Buka file relevan. Hemat konteks — baca yang nyambung saja, bukan seluruh `control/`.

### 3. Code-fallback (kapan turun ke kode)
Turun baca/grep kode **hanya bila salah satu**:
- **(a) Level-implementasi** — pertanyaan menanyakan *bagaimana kode bekerja* (alur fungsi, lokasi endpoint, struktur data nyata), bukan *apa keputusannya*.
- **(b) Knowledge diam/tipis** — file relevan tak ada / tak menjawab (khas brownfield belum `extract`).

Pakai **grep terarah + baca file** (pola sama `plan`/`extract`), bukan scan repo buta. Multi-app → batasi ke app relevan dari `workspace.yaml`. Fallback berat (>2 file) → **dispatch subagent** agar konteks sesi `ask` ramping; fallback ringan → inline. Multi-repo: hanya repo app relevan; bila repo tak ter-checkout lokal, katakan demikian (jangan menebak).

### 4. Jawab + sumber (WAJIB)
Ringkas, langsung. **Selalu sebut asal**: "dari `control/invariants.md`", "dari baca `apps/api/auth.ts:42`". Tiga tingkat keyakinan, nyatakan eksplisit:
- dari **kode** → ground truth.
- dari **knowledge, terverifikasi ke kode** → tertinggi.
- dari **knowledge saja, belum dicek ke kode** → tandai bisa-stale ("ini dari catatan; belum gw cek ke kode").

### 5. Flag drift → route (BUKAN nulis)
Kalau code-fallback menyingkap kode yang **membantah** knowledge:
1. **Flag eksplisit** — sebut keduanya: "`invariants.md` bilang X, tapi `auth.ts` pakai Y — catatannya kemungkinan basi." Jawab pertanyaan dengan **kode sebagai kebenaran** (kode = realita), tapi tunjukkan konfliknya.
2. **Tawarkan koreksi → route ke skill pemilik** (`ask` **tidak** menulis):

| File drift | Skill pemilik (route) |
|---|---|
| `invariants.md` · `workspace.yaml` (stack/capability) · `conventions.md` | `/architect` |
| `business/*` · perilaku fitur (`features/*/business.md`) | `/intake` (di dalam `/feature`) atau `/feature` |
| `integrations.md` (vendor) | `/add-integration` |
| `features/*/feature.yaml` status | lifecycle terkait (`/build` · `/ship` · `/drop`) |

Jelaskan file mana yang punya & skill mana yang memperbaikinya, lalu berhenti. **Jangan menulis koreksi sendiri** — menjaga ownership bersih (tiap file `control/` hanya disentuh skill pemiliknya, yang punya gate/`critic`-nya).

### 6. Saran skill (pointer, BUKAN auto-jalan)
Kalau pertanyaan menyiratkan aksi → beri pointer:
- mau nambah kapabilitas/fitur → `/feature`
- kedengeran bug (perilaku ada tapi salah) → `/fix`
- brownfield, knowledge kosong → `/extract` bakal isi `business/`

**Jangan** meng-invoke skill itu; user yang memutuskan.

## Guardrails
- **Read-only mutlak.** Tidak pernah Write/Edit. Tidak menyentuh `control/` maupun kode. Satu-satunya efek = teks jawaban + (opsional) pointer skill.
- **Anti-ngarang.** Knowledge diam **dan** kode tak ketemu → bilang "belum tercatat di `control/` dan gak ketemu di kode" + sarankan skill pengisi. Jangan menebak (selaras `rules/anti-yes-man.md`).
- **Scope = produk** (lintas semua app di `workspace.yaml`), bukan metodologi plugin. Pertanyaan soal "cara nambah app" dsb → beri pointer skill (`/add-app`), jangan jelaskan internal plugin.
- **SHAPE-only untuk sensitif.** Jangan tampilkan nilai secret/PII/data kartu; cukup nama env var / bentuk kontrak. Saat code-fallback kebetulan melihat secret, jangan kutip nilainya.

## Catatan
- Sumber kebenaran = `control/`; **kode = realita** saat keduanya beda (drift).
- `ask` pelengkap **read-side** dari `render-docs` (knowledge→HTML satu-arah, untuk stakeholder). `ask` = tanya-jawab interaktif, untuk siapa saja, kapan saja.
````

- [ ] **Step 2: Verifikasi frontmatter valid & `name` == nama folder**

Run:
```bash
python3 - <<'PY'
import re, pathlib
p = pathlib.Path("plugin/skills/ask/SKILL.md")
t = p.read_text()
m = re.match(r"^---\n(.*?)\n---\n", t, re.S)
assert m, "frontmatter delimiter --- tidak ditemukan"
fm = m.group(1)
name = re.search(r"^name:\s*(\S+)", fm, re.M).group(1)
assert name == "ask", f"name '{name}' != 'ask'"
assert "Trigger —" in fm and "Jalankan dari root produk yang punya control/" in fm, "description tidak mengikuti pola rumah"
print("OK: frontmatter valid, name=ask, description berpola rumah")
PY
```
Expected: `OK: frontmatter valid, name=ask, description berpola rumah`

- [ ] **Step 3: Verifikasi body memuat semua section wajib**

Run:
```bash
for s in "## Prasyarat" "### 1. Klasifikasi" "### 3. Code-fallback" "### 4. Jawab + sumber" "### 5. Flag drift" "### 6. Saran skill" "## Guardrails"; do
  grep -qF "$s" plugin/skills/ask/SKILL.md && echo "ada: $s" || echo "HILANG: $s"
done
```
Expected: tujuh baris `ada: …`, tidak ada `HILANG:`.

- [ ] **Step 4: Commit**

```bash
git add plugin/skills/ask/SKILL.md
git commit -m "feat(ask): skill AMA produk read-only (knowledge-first, code-fallback, flag-drift→route)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Daftarkan `/ask` di dua manifest plugin

**Files:**
- Modify: `plugin/.claude-plugin/plugin.json` (field `description`)
- Modify: `.claude-plugin/marketplace.json` (plugin `description` di `plugins[0]`)

- [ ] **Step 1: Tambah `ask` ke `plugin.json` description**

Di `plugin/.claude-plugin/plugin.json`, di akhir nilai `description`, ganti penutup `, docs).` menjadi sebutan `ask` lalu tutup. Cari substring:
```
fix (lane bugfix dua-mode: in-flight/post-ship, control/fixes/ first-class), docs).
```
Ganti dengan:
```
fix (lane bugfix dua-mode: in-flight/post-ship, control/fixes/ first-class), ask (AMA produk read-only: knowledge-first + code-fallback, flag drift→route), docs).
```

- [ ] **Step 2: Tambah `ask` ke `marketplace.json` plugin description**

Di `.claude-plugin/marketplace.json`, pada `plugins[0].description`, cari:
```
Skills (init→ship + lane bugfix fix), agent, dan rules untuk mengelola produk multi-app secara AI-first.
```
Ganti dengan:
```
Skills (init→ship + lane bugfix fix + ask read-only), agent, dan rules untuk mengelola produk multi-app secara AI-first.
```

- [ ] **Step 3: Verifikasi kedua JSON tetap valid & memuat `ask`**

Run:
```bash
python3 - <<'PY'
import json
for f in ["plugin/.claude-plugin/plugin.json", ".claude-plugin/marketplace.json"]:
    d = json.load(open(f))
    blob = json.dumps(d, ensure_ascii=False)
    assert "ask" in blob, f"'ask' tidak ada di {f}"
    print(f"OK valid JSON + memuat 'ask': {f}")
PY
```
Expected: dua baris `OK valid JSON + memuat 'ask': …`

- [ ] **Step 4: Commit**

```bash
git add plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json
git commit -m "feat(plugin): daftarkan skill ask di plugin.json + marketplace.json

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Update README — bagian "Tanya" + Status

**Files:**
- Modify: `README.md` (sisip section baru sebelum `## Dokumentasi`; tambah `ask` ke `## Status`)

- [ ] **Step 1: Sisipkan section "Tanya produk" sebelum `## Dokumentasi`**

Di `README.md`, cari baris:
```

## Dokumentasi
```
Ganti dengan (sisip section baru di atasnya):
```

## Tanya produk (read-only)
```
/ask <pertanyaan>   # tanya APA PUN soal produk (greenfield/brownfield): knowledge-first, code-fallback
```
`ask` baca `control/` dulu (sumber kebenaran), turun ke kode cuma saat level-implementasi / knowledge tipis. Tiap jawaban sebut sumbernya; pas nemu catatan basi vs kode (drift) di-flag lalu di-route ke skill pemilik (`/architect`, `/intake`|`/feature`, `/add-integration`). **Tidak pernah nulis** — cuma jawab + (opsional) saran skill. Pelengkap read-side dari `render-docs`.

## Dokumentasi
```

> Catatan: blok ` ``` ` di dalam contoh di atas adalah bagian dari teks README — tulis apa adanya.

- [ ] **Step 2: Tambah `ask` ke baris `## Status`**

Di `README.md`, di akhir paragraf `## Status`, cari akhir kalimat:
```
**Lane bugfix:** `fix` (dua-mode in-flight/post-ship, `control/fixes/` first-class, work-item generalization `build`/`ship`).
```
Ganti dengan (tambah satu kalimat di belakangnya):
```
**Lane bugfix:** `fix` (dua-mode in-flight/post-ship, `control/fixes/` first-class, work-item generalization `build`/`ship`). **Sisi-baca:** `ask` (AMA produk read-only — knowledge-first + code-fallback, grounding wajib, flag drift→route ke skill pemilik, tidak pernah nulis).
```

- [ ] **Step 3: Verifikasi README memuat `/ask`**

Run:
```bash
grep -q "/ask <pertanyaan>" README.md && grep -q "## Tanya produk" README.md && echo "OK README updated"
```
Expected: `OK README updated`

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs(readme): tambah /ask (tanya produk read-only) ke README + status

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Update spec induk §17 — hitungan skill 18→19 + entri `ask`

**Files:**
- Modify: `docs/superpowers/specs/2026-05-24-ai-first-boilerplate-design.md:297`

- [ ] **Step 1: Edit baris daftar skill di §17**

Cari baris (sekitar line 297):
```
- **Skills (18):** `discovery` · `init` · `architect` · `wire` · `add-app` · `add-package` · `add-integration` · `extract` · `feature` (→ `intake` · `fanout` · `plan`) · `breakdown` · `build` · `ship` · `drop` · `render-docs` · `fix`
```
Ganti dengan:
```
- **Skills (19):** `discovery` · `init` · `architect` · `wire` · `add-app` · `add-package` · `add-integration` · `extract` · `feature` (→ `intake` · `fanout` · `plan`) · `breakdown` · `build` · `ship` · `drop` · `render-docs` · `fix` · `ask`
```

- [ ] **Step 2: Verifikasi hitungan & entri**

Run:
```bash
grep -q "Skills (19):" docs/superpowers/specs/2026-05-24-ai-first-boilerplate-design.md && grep -q "\`fix\` · \`ask\`" docs/superpowers/specs/2026-05-24-ai-first-boilerplate-design.md && echo "OK spec induk updated"
```
Expected: `OK spec induk updated`

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/2026-05-24-ai-first-boilerplate-design.md
git commit -m "docs(spec): induk §17 — daftar skill 18→19 (tambah ask)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Smoke akhir — konsistensi seluruh perubahan

**Files:** (tidak ada perubahan; verifikasi saja)

- [ ] **Step 1: Cek jumlah folder skill = 19 dan `ask` ada**

Run:
```bash
n=$(ls -d plugin/skills/*/ | wc -l | tr -d ' '); echo "folder skill: $n"; ls -d plugin/skills/ask/ && echo "ask ada"
```
Expected: `folder skill: 19` dan `ask ada`.

- [ ] **Step 2: Cek tidak ada skill lain ikut berubah (ask = read-only, tak menyentuh komponen lain)**

Run:
```bash
git diff --stat e287ec3..HEAD -- plugin/skills/ | grep -v "plugin/skills/ask/" || echo "OK: hanya plugin/skills/ask/ yang berubah di area skills"
```
Expected: `OK: hanya plugin/skills/ask/ yang berubah di area skills` (tidak ada baris skill lain).

- [ ] **Step 3: Ringkas git log perubahan**

Run:
```bash
git log --oneline e287ec3..HEAD
```
Expected: empat commit (`feat(ask)`, `feat(plugin)`, `docs(readme)`, `docs(spec)`) — plus commit spec desain `206bc49` bila dihitung dari sebelum implementasi.

---

## Self-Review (diisi penulis plan)

**Spec coverage:**
- §4 alur knowledge-first/code-fallback + tabel klasifikasi → Task 1 body (### 1, ### 3). ✅
- §5 grounding wajib + drift-flag + route-ke-pemilik → Task 1 body (### 4, ### 5). ✅
- §6 guardrails (read-only, anti-ngarang, scope, SHAPE-only) → Task 1 body (## Guardrails). ✅
- §7 trigger + prasyarat `control/` → Task 1 frontmatter + `## Prasyarat`. ✅
- §8 dampak komponen (skill baru, plugin.json, marketplace.json, README, spec induk; **tak ubah skill lain**) → Task 1–4 + Task 5 step 2 guard. ✅
- §9 open question "code-fallback via subagent" → diadopsi sebagai aturan ">2 file → subagent" di ### 3. ✅

**Placeholder scan:** tidak ada TBD/TODO; semua isi file ditulis penuh; semua perintah verifikasi konkret. ✅

**Type/naming consistency:** nama skill `ask` konsisten di SKILL.md, plugin.json, marketplace.json, README, spec induk; tabel route menyebut skill yang memang ada (`/architect`, `/intake`, `/feature`, `/add-integration`, `/build`, `/ship`, `/drop`, `/extract`, `/add-app`). ✅
